import { CreditDebitMaskPipePipe } from './credit-debit-mask-pipe.pipe';

describe('CreditDebitMaskPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CreditDebitMaskPipePipe();
    expect(pipe).toBeTruthy();
  });
});
